import React from "react";

type HomeProps = {
};
type HomeState = {
};

export default class Home extends React.Component<HomeProps, HomeState> {
    state: HomeState = {};
    render() {
        return (
            <div>
                Home
            </div>
        );
    }
}